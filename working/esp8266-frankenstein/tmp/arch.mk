# Automatically generated. Not recommended to edit
ifeq ($(CONFIG_ARCH_8051),y)
ARCH=8051
endif

ifeq ($(CONFIG_ARCH_ARM),y)
ARCH=arm
endif

ifeq ($(CONFIG_ARCH_AVR),y)
ARCH=avr
endif

ifeq ($(CONFIG_ARCH_ESP8266),y)
ARCH=esp8266
endif

ifeq ($(CONFIG_ARCH_GBZ80),y)
ARCH=gbz80
endif

ifeq ($(CONFIG_ARCH_MIPS),y)
ARCH=mips
endif

ifeq ($(CONFIG_ARCH_MSP430),y)
ARCH=msp430
endif

ifeq ($(CONFIG_ARCH_NATIVE),y)
ARCH=native
endif

ifeq ($(CONFIG_ARCH_PIC32),y)
ARCH=pic32
endif

